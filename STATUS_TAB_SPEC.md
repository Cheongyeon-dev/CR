# 상태 탭 레이아웃 확정 (MainTerminal type="main")

## 원칙
- 구조·슬롯 **매 턴 항상 렌더**. 빈 값 = `—`
- **반투명 라운딩 박스 금지** (심리·NPC·일기·일정)
- **📅 달력 이모지 금지**
- HUD 외곽 `#040d1a` 디지털 박스만 유지

## HUD (2행 그리드)
```
7월 4일 월요일 09:00              🌞 낮  🌤 맑음
📍 위치                           🌊 파고·풍 | 해역  ☁️ 안개
```
- 1행 좌: 날짜만 (`dateTime`, Day N 대체 금지)
- 1행 우: `dayPhase` + `weather` (안개 괄호 붙이지 않음)
- 2행 좌: `location`
- 2행 우: `seaState` + `seaRegion` + `fogLevel`(☁️)
- `seaState`: **한 문구만** (예: `풍랑주의보`, `거센 파도`, `잔잔한 파도`). `A · B` 병기 금지
- 빈 값: `—` (JSX가 임의 문구 넣지 않음 — AI가 props로 채움)
- `seaRegion` 있으면 `seaState | seaRegion` (해역만 `|`로 구분)

## 복장 / 소지품 | 심리
- 복장·소지품 **행 간격 동일** (10px), 긴 텍스트 줄바꿈
- 심리: 기존 JSX·문구 유지, **영역 ~70%** (이모지·글자 축소)
- 심리 **박스 배경 없음**

## NPC
- **`NPC` 라벨** 필수
- 상태줄 **14px** (옷·소지 12px)
- 형식: `이름|🩵|🍸|🙂기분💭위치&행동` + 들여쓰기 `— 옷 · 소지`
- 빈 슬롯: `—|🩵—|🍸—|🙂—💭—&—`

## 하단 (2열, 세로 늘리지 않음)
| 좌 | 우 |
|----|-----|
| ✒️ {diaryNpcName}의 일기 + 본문 | **예정 일정 \| …** 한 줄 |

- 제목(`…의 일기` / `예정 일정`): `#7fd4df` + **bold**
- 본문: `#f5f0e6` (제목과 구분)
- 일정: `예정 일정 | 19:00 …` 한 줄 유지 · **흰/아이보리 박스 없음**

## 건드리지 않음
- 헤더, 탭, 지도/수집품 탭
- 심리 상태 문구·sanity 분기
- NPC 백틱 형식 자체
